# 10. 原型与本分支实测记录

## 1. 2026-08-22 历史原型（已被最终方案取代）

在独立 worktree、Cloudflare staging 和 `primelab-win` 上，旧原型曾使用 R2 跑通 Windows `0.8.1-hotupdate.0 → .1`：

- 候选安装包 `185,693,447` 字节，SHA-256 `0f76d2747ac5163e3ba08488842ee448fc6b452b1cc9c43b31f97f57353af493`。
- Core `0.20.0`，Runtime `0.20.0-cn.9`，revision 9。
- 正确 token/check 得到候选；无 token 401；Range 206；pause 后 204。
- Windows UI 完成下载进度、Tauri 验签、NSIS passive 安装、退出和自动重启。
- 安装后注册表/ProductVersion/FileVersion 为 `.1`，Core 9120 正常，Runtime `current.json` 未降级或重置。
- 测试结束 release 已 `paused, 0%`；未操作公开 tag、GitHub Release、Landing 或 stable。

该记录证明原有 Tauri 安装路径和 Windows 进程边界可行，但不能证明现在的 GitHub唯一源/Cloudflare缓存方案已经 E2E 通过。旧 R2 路由、key 和 bucket 不再是现行架构的一部分。

历史关键提交：

| 提交 | 内容 |
|---|---|
| `3cf54cad` | Tauri 签名壳更新、兼容矩阵和 Runtime 降级保护 |
| `ac9e20f3` | 旧 Cloudflare staging 原型 |
| `e1c4748c` | 原型文档和上线方案 |

## 2. 2026-08-23 最终方案分支实现

分支：`codex/hot-update-prototype`；本轮起点 `726dff7287bd5cc10c461553ceb20f0806379a15`。

已实现：

- Rust：控制面唯一发现、metadata/兼容校验、Authorization 清除、受限 GitHub fallback、pending 缓存、安装前复核和事件。
- 凭据：一次性邀请、keyring、stable installation ID。
- Web：60 秒/12 小时±30分钟、下载后重启/稍后、来源诊断、邀请导入。
- Cloudflare：D1 GitHub origin 迁移、控制 Worker、无对象存储下载 Worker、Workers Caching/Range 设计。
- CI/CD：正式四平台、Nightly Windows、受保护管理工作流和发布 provenance 脚本。

## 3. 本轮代码级验证

已通过的针对性命令应在每次变更后重新记录；当前记录包含：

```text
cargo test app_update::tests --lib                 10 passed
cargo test update_config::tests --lib              13 passed
web targeted vitest                                23 passed
hot-update control Worker tests                    11 passed
release mirror Worker tests                         7 passed
control CLI / artifact naming tests                 8 passed
pnpm typecheck                                      passed
cargo check                                         passed
cargo fmt --check                                   passed
actionlint v1.7.12                                  passed
Wrangler dry-run（control + mirror）                 passed
git diff --check                                   passed
```

本记录是最小范围验证，不宣称全量单元测试或四平台构建通过。提交后的 clean status 以本轮交付记录为准。

## 4. 已部署的 staging 与真实网络证据

2026-08-23 已部署且只部署 staging：

- 下载镜像 Worker version `21b3072c-b3f9-4ded-9381-91060357744f`。
- 控制 Worker version `ef5e9824-112e-47d5-a9b7-ca77a4ea6684`。
- D1 `0002_github_origin.sql` 已追加应用；旧候选 `prototype-win-0.8.1-hotupdate.1` 保持 `paused, 0%`，GitHub/mirror 新字段为空，因此不会被新控制面下发。
- `GET /health` 为 200；stable 检查无候选为 204；两者均 `Cache-Control: no-store`。
- 含 `+` 的资产路径被 404 拒绝，Worker 不能退化为开放代理。
- 对现有公开 `v0.8.0-rc7` Windows 安装包做 `bytes=0-1023` Range：总大小 `185,478,929` 字节，两次均为 206，第一次 `CF-Cache-Status: MISS`、第二次 `HIT`，并带 `X-Mirror-Upstream: github`。

`primelab-win` 的只读/基础设施复核：

- PowerShell 5.1，Git 2.55.0.windows.2，Node 24.8.0，pnpm 9.15.0，Rust/Cargo 1.97.1。
- 当前 Desktop `0.8.1-hotupdate.1`，managed Core 9120 返回 `0.20.0`，Runtime 为 `0.20.0-cn.9`。
- GitHub self-hosted runner ID 22 在线且空闲，运行于交互式 Session 1；没有开放入站更新端口。
- `hot-update-staging` Environment 已配置 endpoint、镜像、D1、Runtime tag、baseline、专机 token、匹配仓库公钥的 Tauri 私钥和 staging PFX。
- staging Authenticode 是仅供专机 Nightly 的自签名身份，指纹 `BDD4003B8D847A08927D0DC1157561DCD46DC9C5`，有效期至 2027-08-23；它不能用于 canary/beta/stable。

此次只核验已有公开 RC 资产的缓存链路，没有创建或修改 GitHub Release、tag、Landing、公开 stable 或 production Worker/D1。

## 5. 尚未验证/外部阻塞

- 新方案没有获准创建公开 GitHub prerelease，因此未跑 GitHub→Cloudflare→GitHub fallback 的真实安装。
- staging PFX 尚未通过真实 Nightly 产物证明 `Get-AuthenticodeSignature=Valid`；canary/beta/stable 仍缺公开可信 Authenticode 证书。
- macOS 双架构公证、Linux AppImage 更新未跑。
- Nightly/management workflows 尚未在 GitHub Environment 执行。
- GitHub Environment 仍缺最小权限 `CLOUDFLARE_API_TOKEN`；本机 Wrangler OAuth 不能作为 CI 长期凭据。
- 新 Workers/D1 migration 已部署到 staging；production 明确未部署。
- 多运营商网络尚无新方案样本。

这些项目未完成前，只能称为“可测试实现”，不能称为“已上线热更新”。

## 6. 历史本地证据

旧原型截图/安装包曾保存在：

```text
/Users/enzo/Documents/GithubProjects/hermes/CNDesktop/artifacts/hot-update-prototype-20260822/
```

它们不能作为未来候选的发版证据。每次候选必须重新产生 SHA、签名、workflow URL、安装前后版本和网络记录。
