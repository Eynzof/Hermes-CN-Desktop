# Orchestrator TODO

## ORCH-001：沉淀六类典型多 Agent 角色与编排模板

**状态：进行中**

**目标：** 把群聊测试中验证过的角色原型整理为人类可审查、可复制、可验收的 Markdown 编排方案，并在群聊基础能力稳定后完成六类场景的真实验证。

### 文档阶段

- [x] 区分测试角色与用户角色，明确 `qa-failing` 仅用于故障注入；
- [x] 提炼 planner、critic、synthesizer 三个通用角色原型；
- [x] 建立统一角色卡模板；
- [x] 为六类典型场景定义 Profile、房间顺序、逐轮提示词和输出契约；
- [x] 为每个场景补充验收标准、安全边界和常见误用；
- [x] 关联群聊真实运行语义与完整测试指南。

### 验证阶段

- [x] 修复并复测 `BUG-GC-001`：发送后草稿在 500ms 内清空；
- [x] 修复并复测 `BUG-GC-002`：刷新后可继续发送，不再出现 `session not found`；
- [x] 修复并复测 `BUG-GC-003`：20 轮压力场景 UI/REST 均为 45 个 assistant，且无跨成员合并；
- [x] 实现并复测有界 Agent 自动接力：仅回复开头的有效 mention 触发，正文 mention 与 Agent `@all` 不触发；
- [x] 实现并复测群聊级停止：中断当前成员且不再启动剩余成员；
- [ ] 完成 GC-09：使用不同模型执行 A/B 编排；
- [ ] 完成 GC-10：使用秘密哨兵验证 Profile 隐私隔离；
- [ ] 完成 GC-11：使用临时 fixture 执行只读代码 Review；
- [ ] 完成 GC-13：在成员流中途强制断开 WebSocket 并验证恢复；
- [ ] 分别使用确定性假模型和真实模型执行 O-01～O-06；
- [ ] 将结果、耗时、双仓 SHA、截图和日志回填到测试指南。

### 完成定义

只有同时满足以下条件，ORCH-001 才能改为“完成”：

1. 六个场景均有可直接复用的角色卡和逐轮提示词；
2. 所有目标成员的路由、身份、上下文和顺序符合群聊契约；
3. 文档中依赖的非 MVP 路径不存在未关闭的 `FAIL`；
4. 只读、安全和隐私场景有实际证据，不以文字约束代替验证；
5. 真实模型结果只断言角色、路由、引用和结构，不依赖随机措辞；
6. 测试没有修改真实项目、泄漏凭证或产生外部写操作。

### 后续候选

以下内容不属于 ORCH-001，本任务完成后再单独评估：

- 把 Markdown 角色卡转换为可导入的 Profile 模板；
- 在 Desktop 中提供场景模板选择器；
- 在现有 mention 接力之上增加可声明的阶段、条件分支和工作流图；
- 为群聊增加专用压缩和跨 Core 重启持久化；
- 为角色模板增加版本、兼容性和迁移机制。
