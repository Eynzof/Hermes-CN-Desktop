# v0.9.0 Windows 缺陷修复与重新验收记录

最终结果：2026-09-08，cn.10 的 89 个必需工作流全部通过；4 项豁免、8 项隐藏。当前结论见 [最终验收报告](e2e-fixes-acceptance.md)。下文按实际发生顺序保留各轮发现与失败，不代表这些问题仍未修复。

本阶段授权：隐藏 Wander Memory / Wanderminds 账号功能，修复 Windows 缺陷并重新验收；仅本地操作。

双仓工作分支 `fix/v090-e2e-acceptance`。Desktop 基于 652a08a9（含全部原生 E2E），Core 基于 0419cae3ec；未改原集成工作区。Core 原上游贡献者名单的大小写碰撞保留。

## 验收范围变化

- 用户豁免 MODEL-006、CRON-003、IM-FEISHU-001、IM-WEIXIN-001，不再要求账号凭证或真实外部消息投递。
- WANDER-001 至 WANDER-008 随功能隐藏移出功能验收，由 SHELL-006 检查入口与直达路径隐藏。
- 当前源码未包含独立的 Wanderminds / Logto 账号模块。保留模型供应商 OAuth 和远程 Core 的通用认证，以及关于页公司署名。
- 旧安装包 73 通过 / 23 失败仅为历史基线。新构建必须登记新的 Desktop / Core SHA 和二进制哈希，再生成覆盖结论。

## 首轮产品修复范围

- 备份使用 SQLite VACUUM INTO 一致性快照；恢复同名已删除档案时清除删除标记。真实 WAL 导出和恢复回归已通过。
- 版本校验异步等待、过期探测隔离；不可用后端不再触发桌面退出；生命周期状态刷新不反复清空成功校验。
- UI 热更新先完成 IPC 响应再刷新，避免重复回退；Runtime 回滚同步运行期望状态。
- Tauri 默认使用 Rust WebSocket 中继；历史别名与本次连接已挂接会话分开判断；刷新网关时同步运行态。
- 本地模型探测允许空 Key；新增默认模型同步模型选择状态；首页订阅默认模型变化。
- 原生图片强制模式不再被适配器移除；配置保存清空新会话的工具发现缓存，外层缓存遵守可用性 TTL。
- 默认语音依赖、MCP 目录、email/filecmp/Nemo Relay 纳入冻结构建声明。
- MCP 带引号路径解析、OAuth 发起/浏览器/轮询/取消/退出接口与 UI；退出清除本机登录并禁用服务。
- 模型 OAuth 发起期间关闭后清理迟到会话；微信轮询超时 35 秒。
- 记忆删除与档案选择可访问名称；Build 向导标记并锁定必需技能；cron 输出使用唯一文件名。
- 环境凭证元数据纳入当前进程的有效配置，不跨档案读取。

## 完成条件

已用默认首次失败停止和 Remaining 续跑完成 cn.10 的 89 项所需工作流；声音、麦克风、热更新等均在当前产物重新验收。最终覆盖报告和安装包已归档，旧产物记录仅用于追溯。

## 2026-09-07 新安装版验证进展

- Core 修复提交 `253efacc0db74fa93a9d8cfcc186973fffdfdb85`；Desktop 产品修复提交 `b982ef0167b7b699b54519f105781081d67c59c1`。
- Windows 新构建位于 `C:\HermesV090Fixes`，与旧构建独立。Python 按归档核对 Core 10,255 文件 / Desktop 1,351 文件，构建输入无差异（贡献者大小写碰撞名单不参与 Windows 构建）。
- 新安装 EXE SHA256 `3c3ed4f789ae9f2f5654978ef818408c9d5326670a0b304f5ab2763756afc286`；Core `0.21.0-cn.4` ZIP SHA256 `8140127cb8486a370bb5c07fa150efd41823cedb0a308328d235e2159626bf76`。
- 20260907T145403Z：MODEL-001、SHELL-006 两项通过。已人工检查新安装主页面截图。
- 20260907T145509Z 第二批：SHELL-003 冷启动立即真实发送、重启旧会话恢复通过。MEM-001 / BACKUP-001 因修复后删除按钮获得可访问名称，旧脚本还匹配空名称而失败，已改为“删除记忆”，待重跑。RUNTIME-001 / 005 / 006 / 007 均通过真实会话恢复与数据保留验收。
- 独立测试候选 `0.9.1-prototype.local.1` 使用相同修复源码和 Core cn.4，已构建，仅用于整包更新验收。
- 旧更新服务已停止且监听端口为空；旧目录与私钥另存，临时证书按确切指纹移除。新更新夹具将绑定新产物。
- 通知声音：实际交互用户的默认音频设备为 Steam Streaming Speakers，音量 56%、未静音；System Sounds 会话音量 100%、未静音。尚不能解释声音采集为 0，仍需实际触发验证。

### 第二轮发现与修复

- `20260907T150023Z` BACKUP-001 通过；`20260907T150246Z` MEM-001 和 RUNTIME-003 通过。记忆测试使用每次独立的随机暗号，提示词不包含答案；清理恢复原用户文本和容量。UI 热更新与回退期间真实 DeepSeek 任务、原会话续聊均成功。
- WIN-029：Runtime cn.4 ZIP 解压为 560,596,148 字节，超过更新器原有 500 MiB 上限。Runtime 改用独立 2 GiB 上限，UI 继续使用 500 MiB 上限，10 项解压测试通过；等待新安装包原生更新验收。
- `20260907T150817Z` MCP-005 真实带空格路径探测与模型工具调用通过。MCP-003 安装超时后 MCP-004 配置请求也超时。
- WIN-030：对真实 Core PID 11836 的只读 py-spy 线程快照确认，目录安装停在 `_apply_tool_selection → curses_checklist → _read_numbered_input`，持有 `_SKILLS_PROFILE_LOCK` 导致其他模型、技能、MCP 配置请求排队。新增显式非交互安装模式，保留既有工具过滤或目录默认值，不在配置写入阶段连接服务、发起授权或读取 stdin；桌面 API 使用配置档案作用域并串行写入。15 项目录安装测试通过，等待新 Core 构建验收。
- `20260907T152057Z` PROF-002 / PROF-004 / CRON-004 通过。PROF-003 停在旧可访问名称，已适配“hermes-agent 必需”并检查勾选和禁用状态。
- VOICE-003 的 cn.4 失败不是打包依赖缺失：同一 Windows 上同版 edge-tts 7.2.7，en-US-AriaNeural 合成英文成功（17,136 字节），合成中文返回 NoAudioReceived；zh-CN-XiaoxiaoNeural 合成同句中文成功（21,456 字节）。脚本显式选择中文音色并恢复原值，产品补充空音频时的语言/音色提示。
- SHELL-005 中 Core 已被监督器自动拉起，但 UI 缺少完成通知而滞留离线；点击“启动”与监督器竞争又返回忙碌错误。监督器完成后新增状态事件，WebView 刷新实际网关与令牌；验收脚本验证自动恢复，不额外点启动或刷新掩盖问题。等待新 Desktop 构建。

### cn.5 实机验收与第三轮构建

- cn.5 输入：Desktop `3adba6fcac5f8d1b6f06e3f0dbfd2a7acb49abd0`，Core `d8eb63a26c11b02111e36f5a7f85192a36b443bc`。安装 EXE SHA256 `5f5ee7fe3afdc06ef47d5e0100ec3b6b107e3df41ac3ddfee12b258c455efb8a`，Core ZIP SHA256 `3e8809bbfa8858559a13a8605f2552839a91b910ee69d6df9a8ce8ae90076be7`。
- `20260907T160311Z`：BACKUP-001 和 SHELL-005 通过。前者完成 WAL 导出、恢复、删除、再次恢复清除删除标记和原会话实际续聊；后者终止确切 Core PID 后自动恢复，新 PID 与原会话数据均验证，无手动刷新或启动。
- cn.4 的独立验证另已通过中文合成/朗读、Profile Builder、外部终端实际模型调用、远程 Core 认证/恢复、Nous 设备码取消、微信自然到期及刷新、Ollama 无 Key 64K 模型切换。这些历史结果不能替代最终产物的覆盖结论。
- MCP-003 实机报 `list(None)`：官方 Microsoft Learn 目录未指定默认工具时，在写入配置后抛异常。Core `ac36386000e7d119fd84645ae06273803b5271be` 修复空默认值，16 项目录安装测试通过。
- MCP-004 实机错误为 HTTP 500，重定向日志捕获 Starlette `RuntimeError: No response returned`；独立冻结 Core 探测同一认证服务能返回正常错误对象。同步探测线程取消跨越 asyncio 边界会取消 HTTP handler，已将 concurrent.futures.CancelledError 转为可报告异常，1 项跨线程行为测试通过；仍需最终安装包验证。
- Desktop `90e62b1f79536a145a43b0b262b60b84de10fff4`：MCP 重新加载时禁用探测/发起认证，探测失败保留卡片内错误；修复 ZIP 顶层目录中的 Node/TUI 资源解析，实际 cn.5 日志曾误报资源不存在。Web 类型检查和 1 项嵌套载荷 Rust 测试通过。
- VOICE-002 夹具定位：相同本地 Whisper 能正确识别原始 SAPI 中文 WAV，经过 Steam 虚拟麦克风后严重失真。输入设备共享格式 44.1 kHz mono，输出 48 kHz stereo；直接请求不同采样率被驱动拒绝。继续诊断测试输入设备，不能用固定转写文本替代实际录音。
- 通知声音：全局及应用声音开启、请勿打扰关闭。一次实际 UI 测试在 Steam Streaming Speakers 采集到峰值 0.1177，之后短间隔样本为零；前后台对照尚不能排除投递间隔影响。保留实际音频与 toast XML，继续以独立静默间隔后的实际输出验收，不加入模拟提示音。
- 第三轮 cn.6 构建绑定上述 ac363/90e 提交；冻结构建已开始，尚未登记为已安装或验收通过。

### cn.6 安装确认

- Desktop `90e62b1f79536a145a43b0b262b60b84de10fff4` / Core `ac36386000e7d119fd84645ae06273803b5271be` 已装入 Windows 隔离环境并从正常启动器启动。cn.5 自动升级 cn.6，实际 Runtime 路径与源码提交已核对。
- 安装 EXE SHA256 `662854b135e7458d51bf22df5b02d1c6c1e021fab9a86014aa42984b3baec50c`；安装器 `5534b4f7581c4c7fc4853ba5263d9b5def5bd5d8786f2f06f620d76325c1b207`；Core ZIP `b5c66b9dab0f41e7aa9551a34c68407c9b28aea016097702cba74fa9219858da`。
- 新一批 PTY-002（25.9 秒）和 MCP-003（31.1 秒）通过：外部终端实际模型对话、官方 Microsoft Learn 一键安装、真实工具调用与删除均成功。
- cn.5 `20260907T163110Z` 通知声音在 125 秒间隔后仍为 0；不能归因为简单冷却时间，声音开关的实机验收仍未完成。
- Steam Streaming Microphone 的 Windows 格式由原 `1 通道/32 位/44100 Hz` 调到 `2 通道/16 位/48000 Hz`，没有更换默认设备；对齐后仍无法正确识别采集音频。已从 VB-Audio 官网下载 Pack45 并核对有效数字签名，尚未安装，用于下一步排除现有驱动链路问题。
- cn.4 的旧更新夹具及私钥已保存在 `*-previous-cn4`，只移除其确切测试证书。新 Runtime/UI 夹具绑定 cn.6 实际 ZIP，候选 cn.7，临时证书指纹 `7043d43d11aa55a2e730e8059beca39d89a69cb8`。

### MCP OAuth 取消后重试竞争

- cn.6 `20260907T164101Z` 共 3 通过 / 1 失败。SHELL-005 自动恢复再次通过（26.1 秒）。MCP-004 的首次未登录探测已正常返回 HTTP 200 的错误对象，卡片显示错误；取消后的迟到浏览器同意也未生成凭证。
- 接着立即重试授权返回 409：取消接口先标记 error，但 `_run_dashboard_mcp_oauth` 还在清理，服务的 worker_done 尚未置位；前端却已显示取消完成并开放重试。
- Core `12833c5f8a2abfb7c87b06a5c39184bf80f7fee2` 的取消接口等待实际工作线程退出才确认成功；Desktop `02d5edbf854dab208bec5e131c14b90ba3ba3693` 在此期间保持忙碌并处理尚未返回的授权发起请求。1 项真实线程同步 HTTP 测试、2 项前端异步取消测试、Web 类型检查通过；尚未进入安装版验收。
- 下一轮 Runtime 使用 cn.8，避开本轮 cn.7 更新夹具标签。cn.6 安装包和 local.3 候选已保留。

### 音频输入链路恢复

- VB-CABLE Pack45 安装后，PnP 的三个端点均 OK；尚未重启机器，已经能实际录放。默认录音设备为 CABLE Output；默认播放的三个角色仍是原 Steam Streaming Speakers。
- 安装器是自绘界面。一次性视觉点击脚本显式使用 Per-Monitor V2 DPI 上下文，避免 200% 缩放把物理截图坐标映射到托盘；读回命中 HWND 后才执行安装。驱动安装器不随 Desktop 或测试安装流程分发。
- 新夹具使用 WASAPI 48 kHz 双声道的 CABLE Input/Output。实录峰值 0.3608，303,616 帧；同一 Whisper 正确识别“這是語音輸入測試，請指回覆語音測試通過”。脚本接受简繁中文等价字符，实际录音和实际模型回复仍是唯一验收来源。
- 通知对照目录 `C:\HermesV090Fixes\artifacts\audio-session-1788800244865`：交互式桌面与 SSH 同时录到完全一致的峰值 0.1177、RMS 0.0514，实际发声在采集开始后约 0.5 秒；排除了跨会话限制与这次 4 秒窗口不足的假设。调整流程为先验证独立的提示音开启/关闭，再执行批量前后台通知策略，仍要求真实声音通过。

- cn.6 `20260907T170332Z`：VOICE-002 通过（29.1 秒），证明新虚拟输入经过 Desktop MediaRecorder、页面离开停止设备、实际 STT 和官方 DeepSeek 消息发送完整链路可用。SET-006 在独立开启声音样本仍为零，保持失败；接下来记录真实 desktopNotify 输入/返回值对照诊断样本。

### cn.8 安装确认

- Desktop `02d5edbf854dab208bec5e131c14b90ba3ba3693` / Core `12833c5f8a2abfb7c87b06a5c39184bf80f7fee2` 已通过原 NSIS 安装并启动；实际运行 `0.21.0-cn.8`，前一版本 cn.6。
- 安装 EXE SHA256 `129addc3fefb7f2fdd23deda190092a89fdacee43443519546d7cbd322728cfe`；安装器 `0868d972dfc72f611c393f31dd9256abf475c9a0c6b7c58ed9f7d3f10b41c3c8`；Core ZIP `9680d0d4f9ae127fa442757f6c5f92f76b3320b74289b0a85024e89d14b6446d`。
- cn.6 `20260907T170632Z` 的通知失败已捕获实际调用：withSound=true、showSystemNotification=true，系统返回 delivered=true、focused=false；不能归因为 UI 开关未生效。10 秒录音窗口仍无声音。新脚本将系统 WAV 校准移到真正通知开启/关闭样本之后，以对照之前测试声音的影响，实际声音断言保持严格。
- cn.6 更新夹具及私钥另存为 `*-previous-cn6`，新夹具将绑定 cn.8 的确切产物。

- cn.8 最新批次 SET-006 通过（76.2 秒）：系统提示音开启后的真实扬声器录音、关闭静音、前台抑制、后台真实任务完成、后台真实权限审批提醒均成功。独立系统 WAV 校准放在通知样本之后，避免前置播放影响被测样本；没有修改通知产品代码或合成替代音。
- 同一 cn.8 批次 VOICE-002 再次通过（26.1 秒）。MCP-004 后续失败，未计为通过。

### cn.8 刷新失败的实际原因与脚本收紧

- `20260907T171027Z` MCP-004：取消清理、迟到同意、再次拒绝/授权均通过，但模型并未命中已注册 MCP 工具。原断言只核对工具角色中的摘要，错误地把终端发送 HTTP 请求当作 MCP 调用；整项最终失败，没有计入通过数。
- SQLite 中模型使用 terminal/write_file 自行调用并刷新服务，消耗了刷新令牌却未写回 Core 的 token storage。之后连接使用已消耗令牌触发重新授权。这不是已证实的 Core 令牌缓存缺陷，因此没有据此改动 OAuth manager。
- Desktop 授权成功路径只刷新服务列表和管理页探测，缺少对话 gateway 的 `reload.mcp`。已补充真实 gateway 重载，重载结束前保持忙碌；新增 1 项 UI 时序测试，连同已有取消测试共 3 项通过，Web typecheck 通过。待更新安装包验证。
- MCP-004 已收紧为必须命中注册的 MCP 工具名，禁止以终端/文件工具完成此流程；真实发行服务、真实模型和自然过期仍保留。
- `20260907T171850Z` RUNTIME-002 完整通过（77.9 秒）：签名、摘要拒绝、实际更新到 cn.9 夹具、回滚 cn.8 及两次原会话续聊。
- CHAT-001 的旧断言要求 source=bundled；当前 cn.8 由自动内核升级装入，实际 source=update 合法。改为验证 bundled/update 来源加当前基线 Runtime ZIP 的精确 SHA256，版本和提交校验继续保留。

### 批量回归与 MoA 初始化竞争

- `20260907T172044Z` 批量诊断在 run.log 留下文件操作、记忆、档案、备份、权限审批、终端等多项通过记录，但 MoA 预设被删除后新会话仍选择已删除预设，导致 CHAT-008 / SHELL-004 / CHAT-011 后续受污染。已终止测试 runner；没有完整 results.json，本批不纳入正式通过数，CHAT-009 属人工中断。
- 临时修改 Windows fixture 试图暂停后续 worker 未及时生效，随后已恢复为本地正式文件。停止时只结束明确归属的 Playwright runner/worker，保留安装版应用。新 runner 增加 MaxFailures（默认 1）及 Remaining，后续由脚本正常停止和续跑。
- SHELL-001 的旧导航断言仍包含已隐藏 Wander；已适配当前 5 个主导航。ENV-001 的旧 cn.3 固定版本改为当前基线。
- SKILL-002 确认“我的 Skills”已有项目时缺少复制入口，已在该状态的顶部补回入口。
- MoA 独立复测 `20260907T173601Z` / `173832Z` / `174042Z` 均失败。最后一轮从原生 Tauri 事件监听到 config.set 回执：接受确切测试预设、scope=session、confirm_required=false；实际模型回合仍为普通 DeepSeek。CDP 捕捉不到 Rust 中继帧，Tauri 内部 invoke 为只读属性；前两轮空的观察附件不能当作没有请求的证据。
- Core 的新线程回归先失败后通过：预热 build 已捕获旧配置，config.set 在 agent=None 时只写覆盖值并提前成功；旧 build 随后发布旧 Agent。现在等待已开始的 build 结束，再应用真实会话模型切换，未开始构建的草稿继续使用覆盖值。最终 1 项回归通过。
- Desktop 删除 MoA 预设后清除相同新会话记住的选择；脚本增加删除后普通模型真实对话验收，finally 从实际选择器恢复测试基线。
- 当前待打包源码：Desktop `5328dee023e32b468d227fcd18834c0365a65875`（含 MCP 授权后重载、Skill 复制入口、MoA 删除状态）；Core `92d532982c23627ccd7defe88519c462cbbb3656`（模型初始化竞争及冻结 imaplib/smtplib 补全）。下一 Runtime 为 cn.10，避开 cn.9 更新夹具。

### cn.10 安装确认与重点复测

- Desktop `5328dee023e32b468d227fcd18834c0365a65875` / Core `92d532982c23627ccd7defe88519c462cbbb3656` 已通过 NSIS 安装，正常启动器确认实际运行 cn.10（bundled），前一版本 cn.8。
- EXE SHA256 `fd3ebae91731b933a6707070192b2a3913620153c663fd1f2ca9296c65a069ac`；安装器 `16751b4814dfd90994df3bd443d24569cdb97a2bd8497acf3f507293ff552663`；Core ZIP `5daa85d5b3974f2a0ab0202d3bf3d5e173375301124e6fc7c29c895f565a5cbc`。
- `20260907T180414Z` SKILL-002（10.0 秒）、MODEL-005（21.8 秒）通过，含两路参考模型和聚合器、删除预设后恢复普通 DeepSeek 对话。
- 同批 MCP-004 已命中真实注册 MCP 工具并返回正确摘要；严格允许列表遗漏 Core 的 `tool_describe` 导致失败，未执行后半段刷新。只读 SQLite 证实仅调用 `tool_describe` 和该 MCP 工具，没有终端/文件绕行。已将合法描述步骤加入允许列表，并在断言前保存完整调用证据，重跑完整工作流。
- 安装前的托盘正常退出受到同名 tooltip/text 影响；UIA 名称条件查询与完整实时树的结果不同。原生助手改从实际完整树筛选同名 Button，正常托盘退出已成功，后续生命周期验收继续覆盖。
- cn.8 更新服务及私钥另存 `*-previous-cn8`，只移除各自确切证书。新 Runtime/UI 服务绑定 cn.10 实际 ZIP 和保存的 web-dist，候选 cn.11，临时证书 `e117cc418b90b4f18255b1ee420aabfda868f0b8`。
- 同源码整包候选 `0.9.1-prototype.local.5` 已构建、签名并登记；仍为本地验收用途。
- `20260907T180716Z` MCP-004 完整通过（195.6 秒）：取消后重试、真实注册工具调用、自然过期后 refresh token 轮换、退出清除凭证并禁用均验证。cn.10 当前 3 项通过，开始 Remaining 续跑其余 86 项。
- `20260907T181119Z` 通过 9 项，CRON-001 最后删除检查抢先读到旧列表而失败；随后的真实界面已明确删除成功。改为等待服务端列表移除，不重复发送删除操作。
- `20260907T181355Z` 将 cn.10 累计推进至 50 项通过，包括备份恢复、权限审批、档案切换、真实上下文压缩和压缩后随机暗号续聊。CHAT-010 的子任务已按桌面追加指令写入正确内容；任务完成后父会话自动继续核查结果，脚本在父会话仍运行时查找普通发送按钮而超时。场景现明确告知父会话会由用户从桌面追加指令，并等待父会话真实完成通知回合，再发起第二项停止验证。
- `20260907T182636Z` CHAT-010（53.4 秒）、SHELL-003（39.8 秒）、PROF-004（35.8 秒）、CHAT-007（11.7 秒）通过，累计 54 项。KANBAN-001 的专用 Chrome CDP 已退出；任务上次启动时间加 PT12H 与到期相符，LastTaskResult=267014（任务终止）。浏览器启动脚本现可核对已有专用进程并复用，KANBAN-001 / MCP-004 在前置阶段自动调用该脚本，未运行时启动，保留前置证据。
- `20260907T183109Z` 累计推进至 80 项通过，包含看板、两种外置记忆、完整内核生命周期、Runtime 更新和回滚、UI 热更新和回退、外部 Core 认证、真实 Claude Code 委派、崩溃自动恢复，以及 SET-006 实际声音和前后台通知（79.7 秒）。RUNTIME-004 尚未进入更新步骤：预先启动的自有服务被启动脚本当作端口占用拒绝；已与 Runtime/UI 服务一致，核对自有 PID/子进程和健康后允许复用。
- 实际安装的 cn.10 EXE 通过 `__run-script` 独立导入 email.mime.text、email.message、imaplib、smtplib、filecmp，确认 `sys.frozen=true` 及实际包内路径。证据 `C:\HermesV090Fixes\artifacts\r5-frozen-stdlib-probe.json`；这只是冻结依赖验证，不宣称外部邮件收发通过。
- cn.10 安装器与 Core ZIP 已取回本地 `artifacts/v0.9.0-windows-cn10-local/`，分别 213,324,579 和 258,965,567 字节；重新计算 SHA256 与 Windows 基线一致。
- `20260907T184557Z` 整包用例保留失败：断言仍写死 local.1，实际本轮候选为 local.5。已读取基线版本并检查候选安装器 SHA256，其他更新断言不变。
- 后续整包流程已通过（127.8 秒）：错误签名/摘要拒绝、取消、缓存、安装前授权复核、更新器自动启动候选、原会话续聊和原 NSIS 基线自动恢复。VOICE-002（26.8 秒）、MODEL-010（7.5 秒）、飞书二维码（9.8 秒）及微信普通二维码（35.6 秒）也已通过；自然过期与最后的本地模型、图片和隐藏入口检查仍在运行。

- `20260907T184801Z` 完成后累计 88 项通过；微信自然过期（494.1 秒）、本地模型 64K（148.8 秒）及隐藏入口（2.8 秒）通过。CHAT-013 原生阶段仍失败：0.8B 模型选择 read_file。真实 Ollama 日志明确记录原生回合解码 100 个图片 Token，因此旧的原生丢图问题已消除；同轮辅助视觉正确识别随机像素。保留失败，改用固定摘要的 Qwen3.5 4B 验证完整 Agent 上下文下的视觉，不降低零工具和颜色位置断言。
- `20260907T190535Z` MCP-002（36.5 秒）通过新增的精确注册工具名断言；HTTP 服务及其实际解释器子进程停止、离线错误和删除均验证。先前 190423Z 使用带开头锚点的 grep 未收集到测试，退出非零，不计通过。
- `20260907T191045Z` CHAT-013（436.1 秒）保留失败：4B 原生会话工具调用为 0，答案“红色, 绿色, 蓝色, 黄色”与实际随机像素顺序 red/green/blue/yellow 完全相同，但脚本首个等待断言只接受英文。辅助视觉同时成功。现仅规范化中英文等价颜色名，保留四个位置精确比较与零工具要求，单独复跑；不将旧失败改记通过。
- `20260907T191909Z` CHAT-013（135.6 秒）完整通过，coverage 最终为 passed=89、waived=4、hidden=8、complete=true，完整门槛退出 0。最终证据导出为 20260907T192218Z.zip，11 个运行目录、1,848 个文件；本机独立校验 SHA256 与 ZIP 全部成员通过。
- 收尾已恢复官方模型、1M 上下文、自动图片与视觉设置、内置 UI 和 cn.10。实际托盘正常退出应用；测试容器、Wander、Chrome、UIA 和更新服务停止，监听端口为空，两个临时 CA 不再存在。唯一遗留 cron 从 UI 删除。驱动完成窗口的 Invoke/关闭操作未能结束安装器，最后只结束已核对路径的完成进程；驱动和测试数据保留，未重启 Windows。
