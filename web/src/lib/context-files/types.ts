// Re-export the canonical runtime types so web context-file code stays in sync
// with the agent-core ProfileSnapshot schema.
export type { ContextFile, ContextFileSource } from "@hermes/agent-core";
