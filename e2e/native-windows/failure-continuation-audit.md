# 失败流程后续步骤复核

2026-09-07 对 21 项失败工作流逐项核对真实错误堆栈、附件和当前脚本。失败不能等同于“整项没有执行”，恢复测试环境也不能算原功能通过。累计结果仍为 73 通过、21 失败、6 未运行。

| 工作流 | 已执行与尚未到达的边界 |
|---|---|
| BACKUP-001 | 114227Z 完成原生导出/导入及取消、恢复记忆和模型配置、切回 default 并删除测试档案。恢复后的历史仍为 0 条。归档没有 state.db，sessions 下的两个 request_dump 不是聊天备份；检查器现仅列为 sessionFiles，是否可恢复由真实历史断言判断。 |
| RUNTIME-001 | 114227Z 实际停止、离线显示、启动及配置/聊天落盘保留均完成，最后续聊提示 session not found。独立 114633Z 又在停止时关闭 Desktop；异常退出不是必现于同一步。 |
| RUNTIME-005 | 修正旧脚本的 Dashboard PID 断言后，114227Z 仍显示 backend version check has not completed，没有网关成功反馈。后续“重启成功后的续聊”没有前置条件。 |
| RUNTIME-006 | 114227Z 点击重装后 Desktop 关闭；current.json 和 versions 已删除，配置、密钥摘要、测试文件、数据库原回复仍在。没有到达安装完成和续聊。 |
| RUNTIME-007 | 114227Z 完成取消卸载、实际卸载、重新安装、启动和数据保留，最后续聊失败。脚本随后改为从历史列表搜索并点击原会话；114905Z 在卸载时退出，尚未到达这条新续聊路径。 |
| RUNTIME-002 / 003 | 094008Z 分别保留真实 Runtime 更新后续聊失败、回滚时退出，以及 UI 回退重复执行。UI 更新中的真实任务、回退后续聊和嵌入界面恢复均有独立附件；Runtime 退出后的原流程无法继续。 |
| SHELL-005 | 已注入真实 Core 失联并操作修复入口，页面没有完成重连。后续发送依赖重连。 |
| PROF-003 | 必需技能复选框断言为软失败，后续生成档案及真实模型对话已执行。 |
| MCP-003 / 004 / 005 | 分别止于空市场目录、真实受保护服务缺少授权入口、带空格命令启动失败。安装/授权/连接成功后的模型工具调用依赖这些门槛；普通 MCP 真工具调用另有通过用例。 |
| VOICE-002 / 003 / 004 | 已执行实际录音、手动合成和自动朗读入口；冻结 STT/TTS 依赖不可用。成功转写后的发送、成功合成后的停止/播放依赖实际音频或文字，不能以伪造结果补过。 |
| CRON-004 | 三次真实执行及其来源已记录，持久历史只剩两条；已到最终完整性断言。 |
| SET-005 / 006 | 首次外部连接发送失败后仍验证显式再次发送及两种令牌方式；通知已验证前后台、审批和有声/无声采集，有声断言失败。 |
| WANDER-008 | Escape 与迟到完成均执行，保留两个软失败和真实 DeepSeek 调用。 |
| MODEL-010 | 正常取消与发起中关闭均执行；迟到会话未撤销的失败保留，测试创建的两个会话均已清理。 |
| IM-WEIXIN-002 | 普通长轮询超时后仍完成重新生成、像素解码、离页停轮询和凭据未写入；自然过期由独立通过的 IM-WEIXIN-003 覆盖。 |

生命周期脚本现在无论 UI 是否退出都附加 `lifecycle-final-disk-evidence`。通过磁盘快照看到数据仍在，不代表当时的 Desktop 能够续聊。旧版本脚本和失败记录保留在各次运行的源码快照中，当前脚本增加的新步骤不能追溯算作旧结果已执行。

仍需外部前置条件的六项：MODEL-006 的真实 OAuth 账号；IM-FEISHU-001、IM-WEIXIN-001 的测试账号与明确授权接收者；CRON-003 的渠道和接收者；MODEL-009 的额外本地生成模型；CHAT-013 的视觉模型。已询问的账号和专项模型授权尚未收到。现有 OAuth 均未登录，消息渠道均未配置，专用 Ollama 只有嵌入模型。未执行项目保持 not-run。
